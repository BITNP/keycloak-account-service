<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel="stylesheet" href="{{ url_for('static', path='css/bootstrap.min.css') }}" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>{% block title %}{{ app_title }}{% endblock %}</title>
  </head>
  <body>
    <div class="container d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 border-bottom">
      <h5 class="my-0 mr-md-auto font-weight-normal"><a href="{{ url_for("index") }}" class="text-body">网协通行证</a></h5>
      <nav class="my-2 my-md-0 align-items-center row text-muted">
        {% if signed_in is sameas true %}
        <span class="mr-2">{{ name }}</span> <a href="{{ url_for("logout") }}">登出网协通行证</a>
        {% elif signed_in is sameas false %}
        <a href="{{ url_for("index") }}">尚未登录</a>
        {% endif %}
      </nav>
    </div>

    <main class="container">
      {% block alert %}
      {% include 'global-alert.html.jinja2' %}
      {% endblock %}
      {% block content %}
      <div class="content"></div>
      {% endblock %}

      {% block footer %}
      <footer class="mt-3">
        <p>&copy; phy@bitnp.net <a href="{{ url_for("tos") }}">使用条款</a></p>
      </footer>
      {% endblock %}
    </main>

    {% block footer_script %}
    {% endblock %}
  </body>
</html>